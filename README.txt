This application compares two strings and determine if they are permutation of one another.
The user is prompted to input two strings.
Both strings are passed to the permutation function and the function returns either true or false.
The return value is checked and the appropriate message is outputted to inform the user.